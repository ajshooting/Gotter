services:
  gotter:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - "${HOST_PORT:-8080}:${PORT:-8080}"
    volumes:
      - gotter-data:/app/data
    restart: unless-stopped

volumes:
  gotter-data:
